import { PersonStandingIcon } from "lucide-react";

const MenuTitle = () => {
  return (
    <h4 className="flex items-center">
      <PersonStandingIcon size={40} className="text-primary" /> CodiElectro
    </h4>
  );
};

export default MenuTitle;
